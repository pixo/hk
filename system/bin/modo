#!/bin/bash

echo "Running hk-modo $HK_MODO_VER..."
export PYTHONPATH="$HK_COUCHDB:$PYTHONPATH"

if $HK_DEV_MODE
    then
		code="$HK_USER_CODE_PATH"
    else
        code="$HK_CODE_PATH"
fi

/usr/local/soft/modo/$HK_MODO_VER/modo $@