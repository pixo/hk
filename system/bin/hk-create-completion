#!/bin/bash

destination="/etc/bash_completion.d"
origin=$CODEPATH/system/bash_completion.d

if [ -n "$HOMEWORKS" ]
	then
		if [ -d $destination ]
			then 
				if [ -d $origin ]
					then
				    	for file in `ls $origin/hk-*completion` 
		    				
		    				do sudo ln -sfv $file $BASH_COMPLETION_DIR $destination
				    	
				    	done
				else
					echo "You should set a project with the command first eg:'hk-project project-name'. "
				fi
	    else
	    	echo "Directory '/etc/bash_completion.d' doesn't exist"
	    fi
else
		echo "You should set the HOMEWORKS variable."

fi